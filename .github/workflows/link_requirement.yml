# SPDX-FileCopyrightText: 2025 Contributors to the Media eXchange Layer project.
# SPDX-License-Identifier: Apache-2.0

name: Link Requirement Issue

# Force JavaScript actions (e.g., actions/create-github-app-token) to run on Node 24 now.
# This removes the Node 20 deprecation warning and future-proofs the workflow
# per GitHub’s guidance. You can remove this once all used actions ship Node 24 builds.
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true # See GitHub changelog. [1](https://github.blog/changelog/2025-09-19-deprecation-of-node-20-on-github-actions-runners/)

on:
  issues:
    types: [opened] # Option A: only "opened" → avoids the extra (skipped) run on bot edits

permissions:
  contents: read
  issues: write
  repository-projects: write

jobs:
  link-requirement:
    # Only run for Issue Type = Feature (string or object form)
    if: >
      github.event.issue.type == 'Feature' ||
      github.event.issue.type.name == 'Feature'

    runs-on: ubuntu-latest

    env:
      ISSUE_NUMBER: ${{ github.event.issue.number }}
      ISSUE_BODY: ${{ github.event.issue.body }}
      DEFAULT_REQUIREMENTS_REPO: ${{ vars.DEFAULT_REQUIREMENTS_REPO || 'dmf-mxl/mxl-requirements' }}

    steps:
      ###########################################################################
      # (0) Use GitHub App token (org) — ACTIVE
      ###########################################################################
      - name: Create GitHub App token (org)
        id: app_token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ vars.CROSS_REPO_ISSUE_ACCESS_APP_ID }}
          private-key: ${{ secrets.CROSS_REPO_ISSUE_ACCESS_PRIVATE_KEY }}
          owner: dmf-mxl
      - name: Export GH_TOKEN from GitHub App
        run: echo "GH_TOKEN=${{ steps.app_token.outputs.token }}" >> "$GITHUB_ENV"
        # Note: keep an eye on action updates that announce native Node 24 support. [2](https://github.com/actions/create-github-app-token)

      ###########################################################################
      # (1) DEBUG: show a small snippet so we see what we’re parsing
      ###########################################################################
      - name: DEBUG — show body snippet
        shell: bash
        run: |
          echo "First 300 chars of issue body:"
          echo "------------------------------------"
          printf "%s" "$ISSUE_BODY" | head -c 300
          echo
          echo "------------------------------------"

      ###########################################################################
      # (2) Extract requirement_link after the '### Requirement Link' heading
      ###########################################################################
      - name: Extract requirement_link
        id: extract_raw
        shell: bash
        run: |
          CLEAN=$(printf "%s\n" "$ISSUE_BODY" | sed 's/\r//g')

          # Find a line that is exactly the '### Requirement Link' heading (case/space tolerant),
          # then skip blank lines, and take the first non-empty line as the value.
          REQ_LINE=$(printf "%s\n" "$CLEAN" | awk '
            BEGIN { seen=0 }
            tolower($0) ~ /^###[[:space:]]*requirement[[:space:]]*link[[:space:]]*$/ { seen=1; next }
            seen && $0 ~ /[^[:space:]]/ { print; exit }
          ' | xargs)

          echo "Extracted requirement_link: '$REQ_LINE'"
          echo "requirement_link=$REQ_LINE" >> "$GITHUB_OUTPUT"

      - name: Guard requirement_link
        id: guard
        shell: bash
        run: |
          if [ -z "${{ steps.extract_raw.outputs.requirement_link }}" ]; then
            echo "Requirement Link missing — skipping workflow"
            echo "has=false" >> "$GITHUB_OUTPUT"
          else
            echo "has=true" >> "$GITHUB_OUTPUT"
          fi

      ###########################################################################
      # (3) Normalize → owner_repo, issue_num, requirement_url
      ###########################################################################
      - name: Normalize requirement link
        if: ${{ steps.guard.outputs.has == 'true' }}
        id: normalize
        shell: bash
        env:
          RAW_LINK: ${{ steps.extract_raw.outputs.requirement_link }}
          DEFAULT: ${{ env.DEFAULT_REQUIREMENTS_REPO }}
        run: |
          set -euo pipefail
          LINK=$(echo "$RAW_LINK" | xargs)
          OWNER_REPO=""
          ISSUE_NUM=""
          echo "Normalising: '$LINK'"

          if [[ "$LINK" =~ ^[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+#[0-9]+$ ]]; then
            OWNER_REPO="${LINK%%#*}"
            ISSUE_NUM="${LINK##*#}"
          elif [[ "$LINK" =~ ^\#?[0-9]+$ ]]; then
            OWNER_REPO="$DEFAULT"
            ISSUE_NUM="${LINK#\#}"
          elif [[ "$LINK" =~ ^https?://github\.com/([^/]+)/([^/]+)/issues/([0-9]+)$ ]]; then
            OWNER_REPO="${BASH_REMATCH[1]}/${BASH_REMATCH[2]}"
            ISSUE_NUM="${BASH_REMATCH[3]}"
          else
            echo "ERROR: Cannot parse requirement reference '$LINK'"
            exit 1
          fi

          REQUIRE_URL="https://github.com/${OWNER_REPO}/issues/${ISSUE_NUM}"
          echo "Normalized → $REQUIRE_URL"

          echo "owner_repo=$OWNER_REPO"        >> "$GITHUB_OUTPUT"
          echo "issue_num=$ISSUE_NUM"         >> "$GITHUB_OUTPUT"
          echo "requirement_url=$REQUIRE_URL" >> "$GITHUB_OUTPUT"

      ###########################################################################
      # (4) Fetch requirement title (for display under “Link Requirement”)
      ###########################################################################
      - name: Fetch requirement title
        if: ${{ steps.normalize.outputs.issue_num != '' }}
        id: parent_meta
        shell: bash
        env:
          PARENT_REPO: ${{ steps.normalize.outputs.owner_repo }}
          PARENT_NUMBER: ${{ steps.normalize.outputs.issue_num }}
        run: |
          set -euo pipefail
          PARENT_URL="https://github.com/$PARENT_REPO/issues/$PARENT_NUMBER"
          # Use gh issue view to read the title
          TITLE=$(gh issue view "$PARENT_URL" --json title --jq .title)
          echo "title=$TITLE" >> "$GITHUB_OUTPUT"

      ###########################################################################
      # (5) Parent → child sub-issue link (idempotent; full URLs; sub_issues header)
      ###########################################################################
      - name: Link as sub-issue (idempotent)
        if: ${{ steps.normalize.outputs.issue_num != '' }}
        shell: bash
        env:
          PARENT_REPO: ${{ steps.normalize.outputs.owner_repo }}
          PARENT_NUMBER: ${{ steps.normalize.outputs.issue_num }}
          CHILD_REPO: ${{ github.repository }}
          CHILD_NUMBER: ${{ env.ISSUE_NUMBER }}
        run: |
          set -euo pipefail
          PARENT_URL="https://github.com/$PARENT_REPO/issues/$PARENT_NUMBER"
          CHILD_URL="https://github.com/$CHILD_REPO/issues/$CHILD_NUMBER"

          echo "Parent:  $PARENT_URL"
          echo "Child:   $CHILD_URL"

          PARENT_NODE=$(gh issue view "$PARENT_URL" --json id --jq .id)
          CHILD_NODE=$(gh issue view "$CHILD_URL"  --json id --jq .id)

          # Idempotency: read current sub-issues
          set +e
          EXISTING_CHILD_IDS=$(
            gh api graphql \
              -H "GraphQL-Features: sub_issues" \
              -f query='
                query($id:ID!) {
                  node(id:$id) {
                    ... on Issue {
                      subIssues(first:100) { nodes { id } }
                    }
                  }
                }' \
              -f id="$PARENT_NODE" \
              --jq '.data.node.subIssues.nodes[].id' 2>/dev/null
          )
          QUERY_RC=$?
          set -e

          if [ $QUERY_RC -ne 0 ]; then
            echo "WARN: Could not read existing sub-issues (rc=$QUERY_RC). Will attempt to add link."
          else
            if echo "$EXISTING_CHILD_IDS" | grep -Fxq "$CHILD_NODE"; then
              echo "Sub-issue relation already exists; skipping link."
              exit 0
            fi
          fi

          # Try to create the relation (tolerate 'already linked')
          set +e
          gh api graphql \
            -H "GraphQL-Features: sub_issues" \
            -f query="
              mutation {
                addSubIssue(input: {
                  issueId: \"${PARENT_NODE}\",
                  subIssueId: \"${CHILD_NODE}\"
                }) {
                  issue { url }
                  subIssue { url }
                }
              }
            "
          MUT_RC=$?
          set -e

          if [ $MUT_RC -ne 0 ]; then
            echo "WARN: addSubIssue returned rc=$MUT_RC (possibly already linked). Continuing."
          else
            echo "Sub-issue relation created."
          fi

      ###########################################################################
      # (6) Clean body (remove the user's raw link), then append a visible section:
      #     "### Link Requirement" with the *title* hyperlinked to the normalized URL.
      #     We also keep a hidden marker block with just the URL for idempotent updates.
      ###########################################################################
      - name: Update issue body (remove raw link, add titled link section)
        if: ${{ steps.normalize.outputs.issue_num != '' }}
        shell: bash
        env:
          REQUIRE_URL: ${{ steps.normalize.outputs.requirement_url }}
          REQUIRE_TITLE: ${{ steps.parent_meta.outputs.title }}
          CHILD_REPO: ${{ github.repository }}
          CHILD_NUMBER: ${{ env.ISSUE_NUMBER }}
        run: |
          set -euo pipefail

          gh api "repos/$CHILD_REPO/issues/$CHILD_NUMBER" --jq .body > body_current.txt || echo -n > body_current.txt

          # 1) Remove "### Requirement Link" heading and the next non-empty line (user's raw value)
          awk '
            BEGIN { drop=0; consumed=0 }
            function isblank(s){ return (s ~ /^[[:space:]]*$/) }
            {
              if (drop==0 && tolower($0) ~ /^###[[:space:]]*requirement[[:space:]]*link[[:space:]]*$/) {
                drop=1; consumed=0; next
              }
              if (drop==1) {
                if (!isblank($0)) { consumed=consumed+1 }
                if (consumed>=1) { drop=0; next }
                else { next }
              }
              print
            }
          ' body_current.txt > body_stripped.txt

          # 2) Remove any previous marker/titled section we own
          sed -e '/<!--mxl:requirement:start-->/,/<!--mxl:requirement:end-->/d' body_stripped.txt > body_clean.txt
          sed -e '/<!--mxl:linksection:start-->/,/<!--mxl:linksection:end-->/d' body_clean.txt > body_base.txt

          # 3) Append visible titled section + invisible marker block
          {
            echo
            echo "<!--mxl:linksection:start-->"
            echo "### Link Requirement"
            if [ -n "$REQUIRE_TITLE" ]; then
              # Title + URL
              echo "- [$REQUIRE_TITLE]($REQUIRE_URL)"
            else
              # Fallback to just the URL if title could not be read
              echo "- $REQUIRE_URL"
            fi
            echo "<!--mxl:linksection:end-->"
            echo
            echo "<!--mxl:requirement:start-->"
            echo "$REQUIRE_URL"
            echo "<!--mxl:requirement:end-->"
          } >> body_base.txt

          # 4) Only PATCH if something actually changed (avoids spurious edited events)
          if cmp -s body_current.txt body_base.txt; then
            echo "No body changes; skipping PATCH."
            exit 0
          fi

          echo "Patching body…"
          gh api \
            --method PATCH \
            -H "Accept: application/vnd.github+json" \
            "repos/$CHILD_REPO/issues/$CHILD_NUMBER" \
            -f body="$(cat body_base.txt)"
